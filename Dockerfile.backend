FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY app.py models.py import_tools.py ./
RUN mkdir -p /app/instance

EXPOSE 5000

CMD ["python", "app.py"]
